import * as React from 'react';
import { Component } from 'react';
import './MusicBar.scss'

interface Props {}

interface State {}

export default class MusicBar extends Component<Props, State> {
  constructor(Prop: Readonly<Props>) {
    super(Prop)
    this.state = {

    }
  }

  render() {
    return (
      <div className="_music-bar">
        i
      </div>
    )
  }
}